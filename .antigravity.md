# .antigravity.md - Navatation 项目 agy 指令集

<PROJECT_CONTEXT>
本项目已从 Gemini CLI 迁移至 Antigravity CLI (agy)。
核心配置存储在 `.agents/` 目录中。
</PROJECT_CONTEXT>

## 🛠️ agy 特有工作流

### 1. 服务自动化
项目配置了 `SessionStart` 和 `SessionEnd` 钩子。
- 启动 `agy` 时，将自动运行 `scripts/start-be.bat` 和 `scripts/start-fe.bat`。
- 退出 `agy` 时，将自动清理相关服务。

### 2. 权限策略
- `autoExecutionPolicy`: `always` (自动执行安全命令)
- 允许的命令范围：`npm`, `npx`, `mvn`, `git`, `cmd.exe`, `powershell.exe`。

### 3. 技能调用
所有 superpowers 技能已迁移至 `.agents/skills/`，agy 将自动识别并加载。

## 📜 首席架构师指令
在 agy 环境下工作时，请务必保持 TUI 界面的整洁，并利用其交互性进行实时代码评审。
完成后务必运行 `.\push-dev.bat` 推送变更。
