# 📋 navatation 项目开发看板 (dev 分支)

> 维护者：AI 助手| 最后更新：2026-06-08T22:50
> 
> 🔗 关联主线稳定看板：[主线稳定看板 (main 分支)](file:///e:/workspace/navatation/doc/WORKFLOW-STATUS.md)

---

## 当前开发版本状态 (dev 分支)

| 维度 | 状态 |
|------|------|
| 当前开发版本 | v1.0 MVP + 推荐网址批量管理与代码规范化重构（增量已推 ✅） |
| 数据库 | DDL 已执行 ✅ (7 张表已建) |
| 后端进度 | 用户认证 API ✅ \| 导航管理 API ✅ \| 设置 API ✅ \| 待办事项 API ✅ \| **推荐网址批量管理 API ✅** |
| 前端进度 | 项目初始化 ✅ \| 主页面 UI ✅ \| Service 层 ✅ \| 组件 ✅ \| 登录联调 ✅ \| 待办面板 ✅ \| 拖拽排序 ✅ \| 主题切换 ✅ \| **聚合AI搜索层 ✅** \| **效率专注组件体系 ✅** \| **推荐网址批量管理 UI ✅** |
| 测试状态 | 默认由用户手动确认，QA 角色保留作为被动指派验证 |

---

## 数据库表结构 (已执行 DDL)

| 表名 | 说明 | 状态 |
|------|------|------|
| `navatation_user` | 用户表 - 注册用户基本信息 | ✅ 已建 |
| `navatation_user_config` | 用户配置表 - 个性化显示设置(搜索引擎/壁纸/图标尺寸等) | ✅ 已建 |
| `navatation_nav_category` | 导航分类表 - 用户自定义快捷方式分组 | ✅ 已建 |
| `navatation_nav_shortcut` | 导航网址表 - 用户添加的网站快捷方式 | ✅ 已建 |
| `navatation_recommend_category` | 推荐分类表 - 系统预置的推荐网站分类 | ✅ 已建 |
| `navatation_recommend_site` | 推荐网址表 - 推荐分类下的具体网站 | ✅ 已建 |
| `navatation_todo_item` | 待办事项表 - 用户轻量级待办任务 | ✅ 已建 |

---

## 后端进度详情 (navatation-admin)

### 项目结构

| 模块 | 说明 | 状态 |
|------|------|------|
| `navatation-business` | 业务主模块 (Controller/Service/Mapper/Entity/DTO) | ✅ 已搭建 |
| `navatation-common` | 公共模块 (Result/BizException/ResultCode) | ✅ 已搭建 |
| `navatation-framework` | 框架模块 (Security/JWT/CORS/Redis/全局异常) | ✅ 已搭建 |

### 技术栈

- Spring Boot + MyBatis-Plus + Redis + JWT + RSA
- 端口: `8080`
- 数据库: `navatation` (MySQL)
- 密钥目录: `navatation-admin/rsa/` (自动生成)

---

## 前端进度详情 (navatation-web - dev 分支)

### 技术栈

- React 18 + Vite + TypeScript + TailwindCSS 4 + shadcn/ui + Radix UI
- 动画: motion (framer-motion)
- 图标: lucide-react
- 拖拽: react-dnd

### 业务组件

| 组件 | 说明 | 状态 |
|------|------|------|
| `App.tsx` | 主页面 (已添加 AI 搜索 Overlay 状态与联动组件) | ✅ UI/功能已完成 |
| `AiSearchOverlay.tsx` | **[NEW]** 聚合 AI 搜索看板（单轨/三轨并列/大师提炼/多轮追问） | ✅ 完美适配毛玻璃质感与流式渲染 |
| `SearchBox.tsx` | 搜索框组件 (已接入 AI 引擎拦截，支持应用内快捷搜索) | ✅ 已接入 |
| `SearchEngineSelect.tsx` | 引擎选择器 (已扩充 ChatGPT、通义千问、豆包、聚合AI搜索) | ✅ 已接入品牌 SVG 图标 |
| `LoginDialog.tsx` | 登录/注册弹窗 | ✅ 已对接 API & 完美深色模式适配 |
| `LogoutConfirmDialog.tsx` | 账号管理与登出确认弹窗 | ✅ 已对接 API & 完美深色模式适配 |
| `AddShortcutDialog.tsx` | 添加快捷方式弹窗 (推荐站点 + 自定义输入) | ✅ UI 完成 |
| `EditShortcutDialog.tsx` | 编辑快捷方式弹窗 | ✅ UI 完成 |
| `SettingsDialog.tsx` | 设置面板 (已支持排版/壁纸草稿存储与批量持久化) | ✅ UI 完成 |
| `TodoPanel.tsx` | 待办详情管理面板（支持复制/一键恢复/自适应矢量光标） | ✅ UI 完成 |

---

## 待完成事项 🔧 (dev 分支)

### 研发
- [x] 开发番茄钟小组件 (Pomodoro Widget)
- [x] 开发呼吸冥想小组件 (Breathe Widget)
- [x] 升级 TopDock 统筹管理所有小组件
- [x] 扩展日历和天气小组件，统一云同步逻辑

### 部署与集成
- [ ] 等待用户确认，执行合并至 `main` 主线分支

---

## 任务历史 (dev 分支)

| 日期 | 任务 | 状态 | 备注 |
|------|------|------|------|
| 2026-06-09 | 修复设置面板滑块拖动归位 Bug (依赖项细化) | ✅ 完成 (已推dev) | 细化了 App.tsx 中游客模式副作用依赖项，改用细粒度稳定 dispatch 函数以避开大对象引用引起的重绘循环；将 SettingsDialog 内草稿重置仅绑定至弹窗开启。 |
| 2026-06-09 | 修复推荐网址左右拖拽卡片异常跳跃 Bug | ✅ 完成 (已推dev) | 将拖拽 Droppable 容器从不支持折行一维的 Grid 布局重构为单行 Flex 横向滚动列表，彻底消除了计算位移差错导致的闪跃问题。 |
| 2026-06-08 | 修复批量管理模式组件引用丢失导致的 React 渲染崩溃 Bug | ✅ 完成 (已推dev) | 修复了在切换到批量模式时，使用 `JSON.parse(JSON.stringify(categories))` 深拷贝导致分类中的 `LucideIcon` 函数组件属性在序列化时被过滤抹除，进而在渲染 `<category.icon />` 时引发 "Element type is invalid" 崩溃的问题。重构为基于 ES6 的 map 和解构进行高保真深拷贝以保留组件引用。 |
| 2026-06-08 | 推荐网址批量管理与后端规范重规及实体包分类 | ✅ 完成 (已推dev) | 前端新增以分类为维度的“批量管理模式”紧凑表格行，支持一键批量保存、嗅探及本地上传；后端新增批量保存 API 端点并使用全量覆盖更新算法，对循环写数据库进行了批量聚合优化，完全契合阿里规约；重构实体类包结构划分为四大业务域，在 Controller 和 DTO 中增加了完备的 `jakarta.validation` 参数校验 and 规范的 Javadoc。 |
| 2026-06-08 | 整理项目目录、合并SQL，并扩展后台测试类 | ✅ 完成 (已推dev) | 彻底清理根目录及前后端下无用的残留/备份文件与增量 SQL 脚本，仅保留唯一 `ddl.sql` 库脚本；在后端 test 包中润色 `PasswordGenTest.java` 并新增 `RsaKeyGenTest.java` 单元测试类，支持在开发/部署中一键生成、备份及刷新前后端 RSA 密钥对。 |
| 2026-06-08 | 隔离管理员设置数据至UserConfig并修复新增卡片保存Bug | ✅ 完成 (已推dev) | 彻底将管理员设置数据从 RecommendConfig 表迁移并归并到 UserConfig 表中，仅以 userId (adminId) 进行隔离；同时游客配置直接拉取该管理员 ID 下的数据，全面统一表结构；并修复了前端在全新或无分类账户下添加快捷方式无法发起保存请求的 Bug。 |
| 2026-06-08 | 优化管理员网址推荐与游客配置完全同步 | ✅ 完成 (已推dev) | 优化管理员推荐网址的交互：直接点击卡片可直接加入待添加列表以添加至首页，只有右上角编辑图标才进行修改；同时后端游客的桌面小组件数据源改为直接拉取管理员的配置，保证游客与管理员配置 100% 保持同步。 |
| 2026-06-08 | 修复管理员修改推荐网址排序不生效的 Bug | ✅ 完成 (已推dev) | 修复了后端 `NavService` 在获取和映射系统预置推荐网址数据（`getRecommended` 和 `addRecommendSite`）时丢失 `sortOrder` 字段的问题。通过在 DTO 中补充传递该字段，解决了管理员在界面上修改推荐网址排序后因未读取导致失效的错误。 |
| 2026-06-08 | 丰富系统推荐网址数据库 | ✅ 完成 (已推dev) | 将国内与国外一批知名网址按分类补充加入到系统预置的推荐网址中，更新了 ddl.sql 初始化脚本并在当前数据库中同步插入生效。 |
| 2026-06-08 | 优化管理员编辑推荐网址交互逻辑 | ✅ 完成 (已推dev) | 修复了管理员在管理推荐网址时，点击网址卡片会将其误添加到个人首页（pendingShortcuts）的问题。现在管理员身份下，点击推荐网址卡片将直接触发编辑动作，纯粹用于管理系统推荐数据，不再与个人主页的添加动作混淆。 |
| 2026-06-08 | 全局支持基于小数的自定义排序插队 | ✅ 完成 (已推dev) | 彻底改造系统底层的排序逻辑，将涉及排序的所有 5 张表（`nav_category`、`nav_shortcut`、`todo_item`、`recommend_category`、`recommend_site`）中的 `sort_order` 字段从 `INT` 升级为 `DOUBLE`，同时同步改造了后端所有涉及实体的 Java 类型及业务逻辑。并在前端支持通过小数（如 1.5）控制精确插队，全面支持颗粒度更细的排序策略。 |
| 2026-06-08 | 优化网址编辑弹窗防误触关闭体验 | ✅ 完成 (已推dev) | 移除了添加与编辑网址弹窗背景遮罩的 onClick 事件，现在只有明确点击“取消”、“保存”或关闭按钮才能关闭弹窗，有效防止输入内容时误触遮罩导致窗口被意外关闭及数据丢失。 |
| 2026-06-08 | 修复管理员编辑推荐网址保存不生效及图标颜色丢失的 BUG | ✅ 完成 (已推dev) | 修复 `AddShortcutDialog.tsx` 中编辑系统预置推荐网址时因为缺失 `categoryId` 导致无法保存的问题（增加拦截提示要求先同步到数据库）；并在对象映射及设置编辑状态时补充传递 `iconColor` 字段，彻底解决修改网址信息后导致图标背景色丢失变白的 BUG。 |
| 2026-06-08 | 修复首页快捷方式网格因文字长度导致间距不等问题 | ✅ 完成 (已推dev) | 优化了首页捷径与拖拽组件的布局，为快捷方式的外层容器设置固定宽度（基于图标尺寸自动扩展），并使用 truncate 优雅截断过长文字，彻底解决 flex-wrap 布局下因为内容长度不一造成的横向网格间距失调问题。 |
| 2026-06-08 | 后端权限与数据源分离重构：管理员操作直接映射推荐表 | ✅ 完成 (已推dev) | 彻底改造 SettingsService 和 NavService，拦截 ADMIN 角色的操作，将其查写请求静默重定向至 RecommendConfig、RecommendCategory 及 RecommendSite 表；PublicService 获取游客配置时固定读取推荐表，实现完全的“管理员配置即默认展示”，简化前端状态管理。 |
| 2026-06-08 | 游客模式配置及管理员推荐网址编辑功能开发 | ✅ 完成 (已推dev) | 后端：引入 User 实体 role 字段，改造 NavService 推荐网址为查库，暴露管理 API；新增 PublicService/Controller 提供游客默认配置。前端：更新权限体系，修改 App.tsx 在未登录时自动拉取并应用配置，重构 AddShortcutDialog 为管理员开放推荐分类及网址的 CRUD 功能。 |
| 2026-06-08 | 局域网登录兼容双模自适应加密（完美支持 HTTP/HTTPS） | ✅ 完成 (已推dev) | 引入 node-forge 库重构加密服务；当浏览器在 HTTP 局域网（非安全上下文）访问导致 crypto.subtle 缺失时，自动降级启用纯 JS 进行无感且匹配的 RSA-OAEP-256 加密。开发服务器改回普通 HTTP 监听，并保留 api 代理及相对 API_BASE 路径以彻底解决 Mixed Content 与跨域问题。 |
| 2026-06-08 | 开启前端局域网访问权限 | ✅ 完成 (已推dev) | 修改 vite.config.ts 的 server 配置，增加 host: true，允许同一个局域网内的其他设备通过局域网 IP 访问前端界面。 |
| 2026-06-07 | 解决悬停 TopDock 时小组件背景闪烁问题 | ✅ 完成 (已推dev) | 在 TopDock 与 TodoListWidget 组件上增加了独立的 GPU 硬件加速层以隔离动画重绘。彻底解决了当顶部 Dock 栏透明度和毛玻璃进行 hover 渐变过渡时，因共用同一渲染合成层从而导致页面静态小组件背景产生关联闪烁的渲染缺陷。 |
| 2026-06-07 | 小组件 CSS 样式独立与背景闪动优化 | ✅ 完成 (已推dev) | 彻底将时钟、日历、天气、番茄钟、冥想 5 大类小组件的背景与边框 CSS 提取为专属类，并与公共元素完全隔离，不共用任何全局样式或变量。同时将静态 3D 渲染加速优化为仅在拖动时动态开启，彻底消除了多个小组件并存时毛玻璃背景闪动的缺陷。 |
| 2026-06-07 | 同步 backup-1127 版本至 dev 分支 | ✅ 完成 (已推dev) | 响应用户需求，将 backup-1127 分支的最新代码改动合并至 dev 分支，保持两分支代码的同步一致性。 |
| 2026-06-07 | 前端目录模块化重构与死代码清理 | ✅ 完成 (已推dev) | 将原本平铺在 `components` 根目录下的 12 个业务组件，按业务域拆分为了 `search`、`shortcut`、`todo`、`auth`、`settings`、`dock` 6 大模块目录。彻底移除了 `shadcn/ui` 中 47 个从未使用的无用组件文件、空 `fonts.css` 以及废弃的 `figma` 目录。重构了所有跨模块调用的相对路径，同时输出了 `frontend-architecture.md` 文档规范。 |
| 2026-06-07 | 扩展日历与天气小组件功能 | ✅ 完成 (已推dev) | 完善了日历小组件的月历视图拖拽添加，并新增了基于 Open-Meteo 无密 API 的精美天气预报组件。所有新增的小组件自动复用了原有后端的 `navatation_user_widget` 云端同步保存链路，无需任何数据库改动即实现多端数据同步漫游。 |
| 2026-06-07 | 修复2级分类图标切换导致面板宽度抖动的问题 | ✅ 完成 (已推dev) | 修复了由激活状态边框导致的 `w-fit` 容器宽度抖动问题。通过为二级分类图标在未激活状态添加 `border border-transparent` 预留透明边框，保证了按钮在各种状态下占据的物理空间严格一致，实现了切换分类时的丝滑过渡。 |
| 2026-06-07 | 优化非编辑模式下小组件开关的视觉深度与识别度 | ✅ 完成 (已推dev) | 彻底重构了二级分类图标的样式逻辑，提取了 `getCategoryBtnClass` 函数。为非编辑模式下的“禁用（隐藏）”状态赋予了强烈的物理凹陷感：降低透明度至 40%、追加暗色背景（`bg-input-bg/30`）、施加内阴影（`shadow-inner`）以及微缩的比例（`scale-[0.96]`），与高亮弹起的“启用”状态形成了肉眼可见的高级物理层次对比，做到了无需额外元素也能一眼辨识开关状态。 |
| 2026-06-07 | 非编辑模式下隐藏三级面板并集成小组件快捷开关 | ✅ 完成 (已推dev) | 将二级分类图标在“非编辑状态”下复用为各个类型小组件的显示/隐藏控制开关！通过点击二级分类的图标即可触发各自对应小组件的启用状态 (`clocksVisible`, `calendarVisible` 等)；此外，非编辑模式下不再渲染展示三级具体的组件样式面板，精简了界面交互层级。 |
| 2026-06-07 | 彻底解决二级分类切换时的文本失帧与抖动问题 | ✅ 完成 (已推dev) | 弃用了容易引发文本次像素抗锯齿重绘与失帧的 `scale-105` 物理形变属性。目前采用纯净高级的纯色块高光、内阴影与透明边框结合的视觉反馈方案，搭配 `duration-300 ease-out` 实现极致平滑无抖动的“类苹果原生分段选择器”交互体验。 |
| 2026-06-07 | 优化2级切换动画及增加非编辑模式下面板禁用交互效果 | ✅ 完成 (已推dev) | 将二级分类图标的悬停动画过度曲线调整为更平滑的 `duration-300 ease-out` 以提升流畅自然感。同时修改 TopDock 逻辑，允许在非编辑模式下呼出小组件面板；并在三级具体样式面板上针对非编辑模式追加了 `opacity-60 grayscale cursor-not-allowed` 的灰色锁定视效，叠加了一层拦截事件的透明遮罩与 Title 提示，实现只看不可用的优雅禁用交互。 |
| 2026-06-07 | 优化级联面板事件冒泡与自适应宽度 | ✅ 完成 (已推dev) | 通过在小组件面板外层透明容器新增 `onMouseMove` 阻止事件冒泡，彻底解决了鼠标在 2、3 级面板移动时误触发 1 级 (TopDock) 鱼眼放大的问题。移除了面板宽度的强制等宽绑定 (`min-w-[320px]` 与 `w-full`)，让 2 级分类栏和 3 级具体功能栏的宽度各自实现自适应 (`w-fit`) 独立包裹，视觉效果更加灵动协调。 |
| 2026-06-07 | 优化小组件级联面板间距与卡片式独立设计 | ✅ 完成 (已推dev) | 将原本一体化玻璃材质的小组件添加面板进行了解耦拆分，二级分类栏与三级功能面板各自拥有独立的玻璃拟态卡片投影外观。两者通过透明外层容器保持 z-index 层级，间距调大为 gap-[30px]（与一级 Dock 按钮到二级分类面板之间的视觉间距完美一致），并借助外层统一 hover 事件机制实现完美的指针划过防抖保留体验。 |
| 2026-06-07 | 修复菜单崩溃Bug | ✅ 完成 (已推dev) | 修复添加小组件菜单由于 `useEffect` 提升造成的 `isClockOpen` ReferenceError 初始化错误，通过修正 React 钩子的声明顺序恢复了渲染。 |
| 2026-06-07 | 添加小组件多级联动分类菜单功能 | ✅ 完成 (已推dev) | 将“添加小组件”图标的单行样式悬浮菜单重构为多级联动悬浮菜单。向下呼出时钟、日历、计时器、冥想四大分类；鼠标在分类上悬停时在其下方进一步呼出样式卡片，支持拖拽与点击添加；并实现日历分类暂无组件的优雅占位提示与防抖重置逻辑。 |
| 2026-06-04 | Widget 生态扩充：番茄钟与冥想组件 | ✅ 完成 (已推dev) | 全新上线支持 25min/5min 状态无缝切换 of Pomodoro 效率组件，以及基于 framer-motion 提供平滑动效 of Breathe 呼吸冥想组件，并统一由 TopDock 菜单统筹分发管理 |
| 2026-06-03 | 修复翻页时钟导致全页组件闪烁问题 | ✅ 完成 (已推dev) | 查明根因为 FlipClock 冠号点使用 Tailwind animate-ping（内部依赖 transform:scale 强制 GPU 合成层更新），导致所有带 backdrop-filter 元素共同闪烁重绘；冠号改用纯 opacity 自定义关键帧（flip-colon-blink），ClockWidget 容器加 isolation:isolate + will-change:transform 隔离渲染层，FlipClock 卡片颜色改为主题 token |
| 2026-06-03 | 深色模式兼容修复：时钟选样弹窗与 TopDock 背景 | ✅ 完成 (已推dev) | 将时钟选样弹窗硬编码黑底白字改为语义化 token（bg-widget-bg/95、text-text-primary、border-widget-border 等），亮暗主题自动适配；修复 TopDock 深色模式下下半部背景色不一致问题（移除 hover 切换背景色、稳定 backdrop-blur-xl、仅保留 opacity 动画） |
| 2026-06-03 | 时钟小组件非编辑态常显与启用/禁用切换 | ✅ 完成 (已推dev) | 时钟小组件不再仅限编辑模式显示，普通模式下也常驻渲染；在 TopDock 时钟图标常驻，编辑模式下 hover 展开添加菜单，非编辑模式下点击切换时钟全局显隐并持久化到 localStorage（navatation_clocks_visible）；图标颜色反映当前状态 |
| 2026-06-03 | 待办项字体放大与时钟样式封装重构 | ✅ 完成 (已推dev) | 调大首页未完结待办清单 (TodoListWidget) 的字体字号为 text-xs；调大待办详情抽屉 (TodoPanel) 任务项字体字号为 text-base；在 theme.css 中统一封装 glass-widget-xl 和 glass-widget-opaque 实用类，重构四类时钟小组件以降低维护成本 |
| 2026-06-03 | 优化时钟小组件背景清晰度与对比度 | ✅ 完成 (已推dev) | 为防壁纸干扰导致时钟字迹与指针不清，将模拟 (AnalogClock) 与数字 (DigitalClock) 背景不透明度及模糊度提升（bg-white/35、dark:bg-black/60，backdrop-blur-2xl），将传统 (TraditionalClock) 调整为高透磨砂（bg-white/90、dark:bg-neutral-900/95，backdrop-blur-2xl）；同时将数字时钟年月日、星期小字字号调大至 text-xs、加深字色为 text-neutral-600 dark:text-neutral-300 并改为 font-normal，大幅优化文本可读性 |
| 2026-06-03 | 网址本地上传图标联动搜索停用及清空 | ✅ 完成 (已推dev) | 修改添加/编辑网址弹窗，在本地上传图标后，停用网址图标探测搜索，清空非上传图标，只保留本地上传图标，清除上传图标后恢复 |
| 2026-05-26 | 聚合AI搜索与对比平台功能开发 | ✅ 完成 (已推dev) | 扩展搜索引擎选择器与输入框拦截机制；创建ChatGPT/通义千问/豆包/聚合SVG品牌图标；编写毛玻璃材质交互Overlay支持单轨专注与三分栏并列流式展示；集成“大师提炼总结”与二次提问功能；全量同步推送至dev分支 |
| 2026-05-26 | Git 双分支自动化研发工作流部署 | ✅ 完成 | 在三仓（主、前、后）部署同步本地与远端 dev 开发分支；设计并新建 scripts/push-dev.bat 实现对话结项自动增量 dev 提交；新建 scripts/main/merge-to-main.bat 合并脚本并规整开发规范至 GEMINI.md |
| 2026-05-26 | 扩展设置面板滑块调节范围 | ✅ 完成 | 增加搜索框-上间距最大值至 600px；拓宽图标-上下间距取值范围为 8px - 120px，提升了高分辨率大屏设备上的布局可调性 |
| 2026-05-26 | 核心功能浅/深模式字体背景色彩大一统重构 | ✅ 完成 | 修复 TodoPanel 损坏语法截断并全局重构；统一待办功能、小组件工具栏（TopDock）、快捷网址图标底色（App/DraggableShortcut）、搜索框大功能（SearchBox/SearchEngineSelect）为全新自适应 HSL 主题色彩系统 |
| 2026-05-26 | 账户管理深色模式适配与主题色封装优化 | ✅ 完成 | 在 theme.css 中封装 glass-panel 和 glass-button 自适应毛玻璃工具类，统一右下角控制按钮外观；重构 LoginDialog 和 LogoutConfirmDialog，解决深色模式下偏白刺眼的视觉缺陷，提供高级磨砂暗色体验 |
| 2026-05-25 | 屏幕背景亮度控制位置及防抖 Hover 交互优化 | ✅ 完成 | 重构亮度卡片位置为垂直 Flex 流布局，零间隙咬合在 TopDock 正下方；引入 200ms 微防抖缓冲机制防止滑入二级滑块时“移出即关”死锁；优化卡片为 bg-black/45 高透磨砂黑精致护眼视觉效果，并与浅色模式完美亮度隔离 |
| 2026-05-25 | 业务 ID 逻辑 UUID 化、自增 row_id 引入与超长内容校验优化 | ✅ 完成 | 全量 7 张表升级引入 row_id 物理自增主键，将逻辑主键升级为带前缀的 VARCHAR(64) UUID（U 用户/UC 配置/TD 待办等前缀）并完成前后端及 JWT 框架重构；在 DTO 层通过 @Size(max=512) 彻底修复待办内容过长导致 500 的 Bug |
| 2026-05-25 | 待办面板排版样式优化与按钮隐藏 | ✅ 完成 | 在待办详情面板中隐藏未完成项删除按钮，并对待办列表项内容应用 line-clamp-5 与 break-all，限制每一项最长换行显示 5 行以优化排版表现 |
| 2026-05-24 | JDK 版本及编辑器配置加固 | ✅ 完成 | 显式在 parent pom.xml 中配置并加固编译参数，并为 VSCode 写入工作区及后端的 `.vscode/settings.json`，彻底将编辑器与构建环境切换至 JDK 17 |
| 2026-05-18 | 工作流初始化 | ✅ 完成 | 建立四角色协作体系 |

---

## 文档同步记录 (dev 分支)

| 日期 | 文档 | 变更摘要 |
|------|------|----------|
| 2026-06-09 | `WORKFLOW-STATUS-DEV.md` | 看板同步：新增修复设置面板调节滑块归位 BUG 的开发记录。 |
| 2026-06-09 | `WORKFLOW-STATUS-DEV.md` | 看板同步：新增修复推荐网址左右拖拽异常跳动 BUG 的开发记录。 |
| 2026-06-08 | `WORKFLOW-STATUS-DEV.md` | 看板同步：新增批量管理一键刷新图标功能与后端防 OOM 队列设计的开发记录。 |
| 2026-06-08 | `WORKFLOW-STATUS-DEV.md` | 看板同步：新增修复批量模式下 React.jsx 元素类型非法报错 BUG 的开发记录。 |
| 2026-06-08 | `WORKFLOW-STATUS-DEV.md` | 看板同步：新增推荐网址批量管理、性能优化与规范化重构的开发记录。 |
| 2026-06-08 | `WORKFLOW-STATUS-DEV.md` | 看板同步：新增整理项目目录、合并 SQL 以及后端单元测试扩展的开发记录。 |
| 2026-06-08 | `WORKFLOW-STATUS-DEV.md` | 看板同步：新增隔离管理员配置表及修复新增网址保存 Bug 的开发记录。 |
| 2026-06-08 | `WORKFLOW-STATUS-DEV.md` | 看板同步：新增优化管理员网址推荐与游客配置完全同步的开发记录。 |
| 2026-06-08 | `WORKFLOW-STATUS-DEV.md` | 看板同步：新增修复管理员修改推荐网址排序不生效 Bug 的开发记录。 |
| 2026-06-08 | `WORKFLOW-STATUS-DEV.md` | 看板同步：新增丰富系统推荐网址数据库的开发记录。 |
| 2026-06-08 | `WORKFLOW-STATUS-DEV.md` | 看板同步：新增优化管理员编辑推荐网址交互逻辑的开发记录。 |
| 2026-06-08 | `WORKFLOW-STATUS-DEV.md` | 看板同步：新增全局支持基于小数的自定义排序插队功能的开发记录。 |
| 2026-06-08 | `WORKFLOW-STATUS-DEV.md` | 看板同步：新增优化网址编辑弹窗防误触关闭体验的开发记录。 |
| 2026-06-08 | `WORKFLOW-STATUS-DEV.md` | 看板同步：新增修复首页快捷方式网格因文字长度导致间距不等问题的开发记录。 |
| 2026-06-08 | `WORKFLOW-STATUS-DEV.md` | 看板同步：新增推荐网址编辑功能与首页同步及样式优化的开发记录。 |
| 2026-06-08 | `WORKFLOW-STATUS-DEV.md` | 看板同步：新增后端权限与数据源分离重构（管理员操作直接映射推荐表）的开发记录。 |
| 2026-06-08 | `WORKFLOW-STATUS-DEV.md` | 看板同步：新增补充弹窗缺失图标导入修复报错的开发记录。 |
| 2026-06-08 | `WORKFLOW-STATUS-DEV.md` | 看板同步：新增修复管理员新增网址无报错捕获白屏的开发记录。 |
| 2026-06-08 | `WORKFLOW-STATUS-DEV.md` | 看板同步：新增修复管理员捷径与推荐功能 BUG 的开发记录。 |
| 2026-06-08 | `WORKFLOW-STATUS-DEV.md` | 看板同步：新增游客模式配置及管理员推荐网址编辑功能的开发记录。 |
| 2026-06-08 | `WORKFLOW-STATUS-DEV.md` | 看板同步：更新局域网登录双模加密（完美兼容 HTTP/HTTPS）的开发与测试记录。 |
| 2026-06-08 | `WORKFLOW-STATUS-DEV.md` | 看板同步：新增开启前端局域网访问权限的配置修改与测试记录。 |
| 2026-06-07 | `base_rule.md` | 规范更新：在开发规范中新增 Git 提交描述字数不超过 40 字的要求。 |
| 2026-06-07 | `WORKFLOW-STATUS-DEV.md` | 看板同步：新增小组件 CSS 样式隔离与背景闪烁优化开发记录。 |
| 2026-06-07 | `WORKFLOW-STATUS-DEV.md` | 看板同步：新增合并 backup-1127 分支至 dev 的操作记录。 |
| 2026-06-07 | `frontend-architecture.md` | 新建文档：编写前端项目模块化重构后的详细技术栈概览、目录树结构、数据流架构与样式体系。 |
| 2026-06-07 | `WORKFLOW-STATUS-DEV.md` | 看板同步：新增前端目录模块化重构与死代码清理的开发记录。 |
| 2026-06-07 | `WORKFLOW-STATUS-DEV.md` | 看板同步：新增扩展日历与天气小组件功能的开发记录。 |
| 2026-06-07 | `WORKFLOW-STATUS-DEV.md` | 看板同步：新增优化小组件级联面板间距与独立玻璃拟态卡片化改动的开发记录。 |
| 2026-06-07 | `WORKFLOW-STATUS-DEV.md` | 看板同步：新增添加小组件多级联动分类菜单的开发记录。 |
| 2026-06-04 | `WORKFLOW-STATUS-DEV.md` | 看板同步：新增番茄钟 (Pomodoro) 和冥想 (Breathe) 效率专注组件的开发记录 |
| 2026-06-03 | `WORKFLOW-STATUS-DEV.md` | 看板同步：调大待办事项小组件与管理抽屉内的字号，并在 theme.css 中封装通用 glass-widget 样式进行组件重构 |
| 2026-06-03 | `WORKFLOW-STATUS-DEV.md` | 看板同步：调优时钟小组件中模拟、数字和传统时钟背景模糊度（backdrop-blur-xl）与不透明度以突出文字 |
| 2026-06-03 | `WORKFLOW-STATUS-DEV.md` | 看板同步：新增添加/编辑网址弹窗中本地上传图标与网址嗅探搜索隔离与清空逻辑开发记录 |
| 2026-05-26 | `WORKFLOW-STATUS-DEV.md` | 新建看板：全新创立 dev 专版开发进度文档，实现开发分支与稳定分支进度的双向彻底隔离 |
| 2026-05-26 | `WORKFLOW-STATUS.md` | 看板同步：新增聚合AI搜索与对比平台开发记录，并显式区分 Git dev 与 main 分支的开发隔离状态 |
| 2026-05-26 | `WORKFLOW-STATUS.md` | 看板同步：记录三仓本地与远端 dev 分支一键初始化及双分支自动提交合并脚本部署的开发记录 |
| 2026-05-26 | `WORKFLOW-STATUS.md` | 看板同步：记录扩展搜索框上边距最大值及图标上下间距取值范围的参数调整记录 |
| 2026-05-26 | `WORKFLOW-STATUS.md` | 看板同步：记录待办、小组件、快捷图标、搜索框等核心功能浅/深模式字体与背景配色大一统自适应重构的开发记录 |
| 2026-05-26 | `WORKFLOW-STATUS.md` | 看板同步：记录全局自适应毛玻璃主题色彩搭配封装及账户管理弹窗深色模式适配优化开发记录 |
| 2026-05-25 | `WORKFLOW-STATUS.md` | 看板同步：记录屏幕背景亮度卡片紧贴小组件以及防抖 Hover 复合交互优化的开发记录 |
| 2026-05-25 | `ddl.sql`, `backend-architecture.md`, `WORKFLOW-STATUS.md` | 看板同步：记录业务 ID UUID 逻辑重塑、row_id 自增引入、超长内容参数校验加固以及建表规则写入架构设计文档的开发记录 |

### 2026-06-08 任务日志：整理项目目录与扩展后台测试类
- **需求背景**：用户要求整理 navatation 项目根目录及前后端的无用临时文件，合并增量 SQL 至统一建表脚本以保持目录整洁；并在后端测试包下维护密码生成与 RSA 密钥对刷新工具。
- **实施细节**：
  - **文件清理**：物理删除了根目录下的 `Fix.class`、`GenHash.java`、`ddl.sql.clean`、`update_pwd.sql`，后端下的 `navatation-admin/update_sort_order.sql` 以及前端下的 `navatation-web/default_shadcn_theme.css`。保留了原有的 Claude 插件配置。
  - **测试类编写**：重构了 `PasswordGenTest.java`；并在 `com.navatation.business` 测试包下新建 `RsaKeyGenTest.java`。该测试用例支持生成 2048 位 RSA 密钥对、备份原有密钥文件并自动覆盖，配合前端 `/auth/nonce` 动态公钥请求，实现了前后端密钥的一键轻松切换。
- **状态**：已完成并测试编译通过。

### 2026-06-08 任务日志：隔离管理员个人主页与推荐网址业务
- **需求背景**：之前管理员所有的主页快捷方式操作都被拦截重定向到了系统推荐网址表，导致管理员无法拥有自己的个人书签，且业务边界模糊。
- **实施细节**：移除了 NavService.java 中 9 处针对首页 CRUD 操作的 isAdmin(userId) 拦截逻辑，让管理员在正常添加书签时直接操作 NavShortcut。同时保留了 ddRecommendSite 等专门的推荐网址管理 API 以供管理员在弹窗中使用。另外，修复了意外引入的 AjaxResult 编译错误。
- **状态**：已完成并测试编译通过。

### 2026-06-08 任务日志：优化管理员网址推荐与游客配置完全同步
- **需求背景**：管理员需要能将推荐网址便捷地添加至自己首页，且不影响推荐网址信息的管理交互；同时游客的桌面小组件与配置要与管理员完全同步。
- **实施细节**：
  - 前端：修改 `AddShortcutDialog.tsx`，管理员点击推荐卡片也触发加入待添加队列，通过点击右上角编辑按钮进行卡片内容编辑。
  - 后端：修改 `PublicService.java` 中获取游客小组件的逻辑，改为直接拉取管理员的 `UserWidget` 记录。
- **状态**：已完成并测试编译通过。

### 2026-06-08 任务日志：隔离管理员设置数据至UserConfig并修复新增卡片保存Bug
- **需求背景**：用户反馈管理员添加推荐网址没有正常保存回显。经排查，原前端代码在全新或无卡片分类的账户（如干净的管理员账户）下，因 categoryId 为 undefined 会直接拦截并跳过批量添加 API 导致数据未持久化。同时，用户要求管理员数据与普通用户表结构相同，用 userId 进行物理隔离，游客直接拉取管理员账户数据。
- **实施细节**：
  - 前端：修改 `useShortcuts.ts` 移除 `if (categoryId)` 限制条件。即便 `categoryId` 为 `undefined` 也会照常向后端发送 `batchCreateShortcuts` 请求，由后端自动为此类账户生成默认分类“常用”并保存卡片。
  - 后端：重构 `SettingsService.java` 中对 `ADMIN` 拦截分支，使其所有 settings 的增删改查同样存入普通的 `UserConfig` 物理表中（用管理员的 userId 进行隔离）。修改 `PublicService.java`，让游客的设置也直接从管理员账户 `UserConfig` 记录中获取，彻底统一了游客与管理员的数据源配置。
- **状态**：已完成并测试编译通过。

### 2026-06-08 任务日志：彻底物理表隔离管理员（Root）数据
- **需求背景**：用户要求管理员账户登录时，与普通用户数据进行物理表级别的彻底隔离。需将用户表结构拷贝一份，使用 root 前缀专门存储管理员数据。
- **实施细节**：
  - 数据库层：执行 DDL 新建了 5 张 `navatation_root_*` 前缀的数据表（涵盖配置、分类、快捷方式、组件、待办事项）。
  - 后端 Entity 和 Mapper 层：新建 `RootConfig`、`RootCategory` 等 5 个 Entity 和对应的 Mapper。
  - 业务层：重构 `SettingsService`、`NavService`、`WidgetService` 和 `TodoService`，检测如果角色为 `ADMIN`，则直接使用 `Root*Mapper` 操作底层 `navatation_root_*` 相关表。
  - 游客默认展现层：`PublicService` 中获取默认数据的 `getGuestConfig` 接口依然保持传入超级管理员（ADMIN）的 `userId` 给前述 Service，因底层自动拦截了 ADMIN 路由，游客现在完美地固定拉取物理隔离后的 `navatation_root_*` 数据展现，确保与用户数据严格隔离。
- **状态**：已完成并测试编译通过。

### 2026-06-08 任务日志：推荐网址批量管理与后端规范重构及实体包分类
- **需求背景**：为提升管理员批量维护推荐网站的效率，设计并实现推荐网址以分类为维度的“批量管理模式”；并且在完成后按照阿里巴巴 coding 规范对后端代码审查优化，包含出入参设计、参数校验、Javadoc 注释、以及将实体类按业务划分子包重构。
- **实施细节**：
  - **前端交互**：在添加网址弹窗的“推荐”页签新增“批量编辑模式”开关，开启后展示紧凑的水平行列表，提供分类内的一键批量保存、新行添加、行删除，以及在单行中快速触发 Favicon 自动探测嗅探与本地图片上传回显。
  - **后端 API 与优化**：新增批量保存接口并增加 `@Valid` 级联校验；在 `NavService` 业务层实现全量差分保存算法，并将原本 for 循环内的 `insert`/`updateById` 单条写数据库操作，重构为利用 MyBatis-Plus 静态工具 `Db` 进行批量聚合写入（`Db.saveBatch` / `Db.updateBatchById`），彻底消除了循环内 DB 交互瓶颈，满足阿里规范。
  - **规范化与参数校验**：新增 DTO 并遵循 `@NotBlank`、`@NotNull` 接口参数规则；在 Controller 及 DTO 中补齐了规范 of 中文 Javadoc 及创建者与创建日期；在测试包下运行了 `mvn clean test`，测试用例全部通过。
  - **实体包路径重构**：将平铺的 15 个 Entity 实体类，按业务内聚重构分入 `user`（用户/基本配置）、`nav`（个人中心导航）、`recommend`（推荐网址数据）、`root`（根/默认主页模板）四个子包下，全局替换并修复了全部引用，实体类划分极为清晰。
- **状态**：已完成并测试编译通过。

### 2026-06-08 任务日志：修复批量管理模式组件引用丢失导致的 React 渲染崩溃 Bug
- **需求背景**：管理员开启“批量管理模式”时发生 React.jsx 元素类型非法（Element type is invalid）导致页面白屏崩溃的运行时报错。
- **实施细节**：定位原因是原深拷贝语句 `JSON.parse(JSON.stringify(categories))` 会过滤抹去 React 属性（即 LucideIcon 的 React 函数组件引用），导致渲染 `<category.icon />` 时为 undefined。重构为通过 map 展开运算符执行浅拷贝/深拷贝，高保真还原了 `category.icon` 组件引用，彻底修复了崩溃问题。
- **状态**：已完成并测试编译通过。

### 2026-06-08 任务日志：优化管理员推荐网址批量管理界面布局与多路并发搜索
- **需求背景**：用户反馈在管理员登陆下，批量管理推荐网址功能由于与左侧的单项“本次添加”无功能交互，因此需要扩大批量管理区域的页面占比，给控制列的刷新和上传按钮留出充足的空间，并实现类似自定义页的搜索图标多线程检测标准功能。
- **实施细节**：
  - **前端视图层优化**：修改 `AddShortcutDialog.tsx`，当开启“批量管理模式”时动态隐藏右侧单项添加面板，将主体部分栅格占比扩展至全宽 (`col-span-3`)。并扩大表格第一列“图标与控制”的尺寸至 180px，为“刷新”和“上传”图标补充了中文文本按钮样式，提升了点击体验。
  - **搜索图标多路并发重构**：将原本单线程依赖后台拉取图标的 `handleDetectRowIcon` 改写为标准的“多路并发（多线程）竞速”逻辑。瞬间响应 Google CDN 兜底，并同时发起 DuckDuckGo CDN 与后台深度爬取请求，率先响应的结果优先进行回调覆盖，显著改善了自动嗅探时的加载速度与容错率。
- **状态**：已完成并测试编译通过。

### 2026-06-08 任务日志：重构批量管理页面为精美的卡片式列表，并实现搜索图标多线程并发选择功能
- **需求背景**：用户反馈虽然扩展了批量管理页面的宽度，但是表格化的布局依然显得“比较丑”，且要求图标刷新功能完全对齐自定义添加功能中的“多端并发+人工自选”的标准体验。
- **实施细节**：
  - **组件视图层全面卡片化重构**：将原本呆板的表格 `grid` 布局彻底摒弃，重新设计为了美观现代的“卡片式”排版。为每一个网址条目配备了独立的带阴影、边框悬停动效与内边距的白底卡片 (`bg-background rounded-2xl p-4 shadow-sm`)，并在卡片内部将输入框组合为紧凑的三列网格结构，极大地提升了页面的视觉品质与高级感。
  - **实现标准的搜索竞速与多图任选机制**：进一步深入改造了图标探测流程。不仅沿用了之前“Google打底 + 后台抓取 + DDG备用”的三线程竞速机制，更引入了全新的 `rowDetectedIcons` 状态管理，从而在每个卡片底部动态渲染出了一个“多图标选择区”。管理员现在可以直观地预览到并发搜集到的所有备选图标，并点击任一图标直接覆写当前选择，达到了与“自定义页”丝毫不差的标准化体验。
  - **超紧凑单行交互布局与新增按钮强化**：响应用户诉求，将原本占据多行的表单和多图标选择区进行了极限压缩。现在，名称、链接、极简排序号与探测到的多图标均优雅地收纳在同一行展示。并且，在每个分类列表的尾部新增了一个极具点击欲望的精美悬停“+ (添加新网址)”大按钮，极大优化了连续录入数据时的交互体验。
- **状态**：已完成并测试编译通过。

### 2026-06-08 任务日志：批量管理新增一键刷新图标功能与后端防 OOM 队列设计
- **需求背景**：用户要求在推荐网址的“批量管理”模式中，新增一键自动刷新全部或分类网址图标的功能，并且要求后端保证抓取性能、防止由于大量 Jsoup 爬虫并发请求而导致的 OOM（内存溢出）。
- **实施细节**：
  - **后端防 OOM 队列设计**：配置专门的有界队列线程池 `faviconExecutor`（核心 5，最大 10，有界队列 200），通过 `CallerRunsPolicy` 拒绝策略为高并发请求提供反压机制。并在接口层限制单次批量最大支持 100 个 URL 并去重后执行，设置 15 秒全局超时控制自动截断，抓取失败自动降级回退，彻底规避了高并发下的 OOM 和 Tomcat 线程池卡死隐患。
  - **前端交互与竞速融合**：前端在批量管理顶部新增全局“一键刷新全部图标”按钮，在分类块头部新增“一键刷新图标”按钮。点击后自动提取有效网址，批量标记 loading 并瞬间拼装 Google CDN / DDG CDN 图标进行本地竞速渲染，同时并行请求后端的批量嗅探接口，接口返回后自动追加回填高清原生图标并设为首选，完美兼容原有的多图标点选机制。
  - **文档更新**：同步更新了接口定义到 `api-specification.md`。
- **状态**：已完成并测试编译通过。

### 2026-06-08 任务日志：优化一键刷新图标并发竞速策略与 5 秒自适应覆盖规则
- **需求背景**：用户要求一键刷新图标能够真实按响应速度从左到右排列，并要求在执行刷新 5 秒后，对所有图标进行智能裁断：如果当前正在使用的图标在探测结果队列中，则不做变更；如果未包含，则自动替换为响应最快的首个图标。
- **实施细节**：
  - **真实的异步竞速加载**：修改 `AddShortcutDialog.tsx`，将批量刷新与单行刷新中的 Google CDN 图标探测机制从同步推入数组改造为真正的 `Image.onload` 异步加载事件。由此确保备选图标队列严格按照 CDN 的真实响应速度从左至右排序（最快的排在最前面）。
  - **五秒自适应兜底决策机制**：在执行“一键刷新图标”（分类/全部）时，触发了一个并行的 5000ms 定时器，并引入 `useRef` 同步获取最新探测的并发图标队列状态（`rowDetectedIcons`）。在 5 秒到达时，程序遍历所有刷新行，判断用户当前选择的 `iconValue` 是否依旧有效，若已失效或根本不存在，则静默且自动地回填为响应最快的备选图标，既保证了容错率又提升了智能化操作体验。
- **状态**：已完成并测试编译通过。

### 2026-06-08 任务日志：修复后台图标强制覆盖及优化网址输入体验
- **需求背景**：用户反馈虽然图标搜集执行了多线程竞速，但界面上只有两个图标，且由于加长了批量管理页面，输入框显得过短。
- **实施细节**：
  - **修复后端图标强制覆盖问题**：排查发现前端在异步接收到后端抓取的原生图标返回时，直接强制调用了 `updateBatchEditSite` 覆盖当前的选中图标，破坏了多路图标按速度排列以及 5 秒兜底的智能校验规则。通过加上 `if (newIcons.length === 1 && !site.iconValue)` 安全校验，使后端图标只在没有任何可用图标时才自动回填，而在通常情况下它只作为备选加入队列中供 5 秒校验与人工点选。由于后端如果抓不到也会返回 Google CDN 图标，原有的去重逻辑使其不会重复展示，这就解释了为何通常只展示2个不重复的图标，现逻辑已彻底梳理完毕。
  - **优化长链接与长名称输入展示**：大幅调宽了批量管理卡片中网址名称和链接的输入框（名称调整为 `w-48`，链接调整为 `w-80`），让较长的 URL 也能被清晰一览无余地查阅。
- **状态**：已完成并测试编译通过。

### 2026-06-08 任务日志：优化推荐网址排序方式（废弃手动赋值改用拖拽生效）
- **需求背景**：用户要求在管理推荐网址时，取消由于手动赋排序序号造成的繁琐操作（删除前端暴露的相关参数传递），转而完全依赖拖拽卡片（预览模式）或上下拖动行（批量管理模式）并点击保存来固化排序。同时后端数据库保留 `sort_order` 字段以便固化物理顺序。
- **实施细节**：
  - **清理前端残留字段**：深入排查并清除了前端 `AddShortcutDialog.tsx` 与 `EditShortcutDialog.tsx` 中向后端请求或组件状态内设定的所有有关 `sortOrder` 的残留传参逻辑，切断了手动干预排序的数据入口。
  - **增强批量模式交互**：在“批量管理模式”的卡片行最左侧补充了 `GripVertical`（拖拽把手）图标样式组件，并在 UI 交互上彻底明确了“上下拖拽行实现排序”的设计意图。
  - **机制保证**：拖动后只需点击页面右上角“保存”按钮，后端的批量保存接口 (`batchSaveRecommendSites`) 会自动根据前端传入的拖动后顺序的数组下标进行 `i + 1` 物理赋权，完美实现了基于拖拽的自适应排序更新机制。
- **状态**：已完成并测试通过。

### 2026-06-09 任务日志：修复推荐网址左右拖拽异常跳跃 Bug
- **需求背景**：用户反馈在设置栏（网址添加弹窗下的“推荐”页签预览模式）左右拖动配置按钮时，被拖动的卡片按钮出现严重的异常跳跃和抖动。
- **实施细节**：
  - **根本原因分析**：@hello-pangea/dnd (react-beautiful-dnd) 库的 `direction="horizontal"` 水平拖拽模式在底层仅支持一维一行的水平列表，不支持多行折行的 `display: grid` 布局（如原本使用的 `grid grid-cols-8`）。当元素折行到多行时，库对 Y 轴方向上的多个 `top` 进行了错误的 X 轴投影计算，导致在跨行或者行内拖拽时产生了疯狂的闪动和跳跃。
  - **重构解决方案**：将预览模式下的推荐网址包裹容器重构为单行 Flex 横向滚动容器 (`flex flex-row items-center gap-6 overflow-x-auto pb-4 scrollbar-none`)，使每个分类下的网址自然排列在一行，从物理上避免了折行现象。
  - **细节加固**：为每一个 Draggable 网址子项容器 and ADMIN 下的“新增网址”按钮容器添加了 `flex-shrink-0` 样式，保证在横向 Flex 容器中无论有多少子元素均不被挤压变形，配合 `overflow-x-auto` 实现了非常丝滑的横向滑动与左右拖拽交互。
- **状态**：已完成并测试编译通过。

### 2026-06-09 任务日志：修复设置面板滑块拖动归位 Bug (依赖项细化)
- **需求背景**：用户反馈在设置面板 (SettingsDialog) 拖动任何调整按钮（如搜索框大小、图标间距等 range 滑块）时，一旦拖动，滑块都会在重载后自动弹回重置为最初 the 默认已保存配置，导致无法调节。
- **实施细节**：
  - **根本原因分析**：
    1. 自定义 Hook `useSettings.ts` 与 `useWidgets.ts` 返回的包含数据和方法的对象，在每次组件重绘时都会生成全新的引用（没有包裹 `useMemo`）。
    2. 父组件 `App.tsx` 内用于在游客模式下初始化拉取超级管理员推荐配置的 `useEffect` 副作用，错误地将 `settingsData` 与 `widgetsData` 整个对象作为了依赖项。滑块拖拽实时预览导致 App 重绘，进而触发了 settingsData 引用改变，从而在拖拽滑块的瞬时重复触发了获取默认游客配置的副作用，瞬间将配置强行归位到初始默认值。
    3. `SettingsDialog.tsx` 中的重置副作用依赖了 `settings` prop，任何时候外部 settings 重置都会强制覆盖弹窗内的本地 draft 草稿。
  - **重构解决方案**：
    1. 在 `App.tsx` 中细化解构：从 `widgetsData` 中解构提取 `setWidgets` 和 `setTempWidgets`，从 `settingsData` 中解构提取 `setSettings`，并在游客配置拉取的 `useEffect` 中以这些稳定的 dispatch 函数替换原大对象。这使得在拖动滑块时，重绘绝对不会重复触发副作用拉取默认配置。
    2. 改造 `SettingsDialog.tsx` 中的 `useEffect` 依赖列表，将其限制为只依赖 `[isOpen]` 状态。确保只有在弹窗**打开的那一瞬间**才重置本地草稿，在已开启的状态下，弹窗内部的草稿始终以用户的手动调节为准，彻底阻断了隐式覆盖的 Bug 链路。
- **状态**：已完成并测试编译通过。
